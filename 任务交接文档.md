# MaaKatana 自动化项目交接文档

> 用途：本文件总结 MaaKatana（Nonogram Katana 数织游戏自动化）项目的当前状态，供另一位 AI 模型接手继续开发。
> 生成时间：2026-08-07
> 框架版本：MaaFramework v5.12.3（MPE 本地桥内嵌 v5.12.2）
> 当前主线任务：**给制造节点（迫击炮/武士刀/木炭/木梁）增加"数量识别"分支，实现达到上限或材料不足时跳过该产品、去制造下一个产品。**

---

## 一、项目结构与关键路径

| 用途 | 路径 |
| --- | --- |
| 项目根目录 | `F:\JUSTFORFUN\MaaKatana-main` |
| pipeline 源码（MPE 编辑对象） | `F:\JUSTFORFUN\MaaKatana-main\assets\resource\pipeline\Pipeline6.json` |
| pipeline 运行时副本 | `F:\JUSTFORFUN\MaaKatana-main\install\resource\pipeline\Pipeline6.json` |
| 识别图片目录 | `F:\JUSTFORFUN\MaaKatana-main\assets\resource\image\`（同时复制一份到 install 对应目录） |
| OCR 模型 | `F:\JUSTFORFUN\MaaKatana-main\assets\resource\model\ocr`（含 det.onnx / rec.onnx / keys.txt） |
| MPE 本地桥调试日志 | `C:\Users\yilin\AppData\Roaming\MaaPipelineEditor\LocalBridge\logs\maafw.log` |
| MaaFramework 流水线协议官方文档 | `https://raw.githubusercontent.com/MaaXYZ/MaaFramework/main/docs/zh_cn/3.1-任务流水线协议.md` |

**铁律：**

1. 每次修改 pipeline 后，必须把 `assets\resource\pipeline\Pipeline6.json` 同步复制到 `install\resource\pipeline\Pipeline6.json`，并校验 JSON 合法（`ConvertFrom-Json`）+ 检查 `next` / `on_error` 是否有悬空引用。
2. 文件头部保留 MPE 元数据 `$__mpe_config_Pipeline6`（含 `$__mpe_code`，记录坐标模式、文件路径、视图缩放等，版本 v1.7.4），**不要删除**，否则用户导入 MPE 时会出问题。
3. 项目用 Pipeline v2 格式：`"recognition": { "type": "...", "param": {...} }`。

## 二、环境与工具

- Windows + PowerShell；`F:\` 盘读写需要提权（用户已在 Codex 权限设置中设为 "approve for me"，直接 `require_escalated` 即可）。
- Python 3.14.6；`maa` Python 包已安装于 `C:\Users\yilin\AppData\Roaming\Python\Python314\site-packages\maa`；MaaDebugger 已安装（`python -m MaaDebugger`）。
- 用户用 MPE（MaaPipelineEditor）编辑与调试 pipeline；MPE 本地桥内嵌 MaaFramework v5.12.2。
- 游戏画面分辨率 1280x720。**若日志中出现识别框为全屏 `[0,0,1280,720]`，说明识别配置被错误解析（通常是内联对象问题），属于异常。**

## 三、踩过的坑（务必避免）

1. **MPE 本地桥 v5.12.2 的 `And.all_of` 不支持内联对象定义。** 内联的 FeatureMatch / OCR 会被错误解析成 DirectHit，导致识别框变成全屏、秒命中（用户已通过日志确认此问题）。**必须用节点名称引用**：

   ```json
   "recognition": {
       "type": "And",
       "param": { "all_of": ["点击广告图片确认", "点击广告文字确认"], "box_index": 1 }
   }
   ```

2. OCR 的 `expected` 是正则表达式（boost::regex），支持反向引用 `\1`，但**无法做数字大小比较**。
3. **自定义识别器（`Custom`）无法在 MPE 本地桥 / MaaDebugger 中调试**，只能通过自己的 Python 驱动脚本注册并运行。
4. MPE 调试时部分动作（如点击）会被替换为 DoNothing，实机运行才会真正点击；判断流程是否走对应以日志中的识别命中记录为准。
5. 日志分析教训：某次 23:22 调试约 11 秒"很快结束"是**正常自然结束**——屏幕上没有"正在处理.png"加载图，加载分支被正确跳过；"返回公会"是终点节点，执行完任务即结束，并非中途失败。判断异常前先核对执行路径与识别命中。

## 四、当前 Pipeline6.json 状态

### 4.1 广告奖励流程（已完成并验证通过）

```
进入公会 → 进入交易所 → 寻找广告 → 点击广告 → 等待广告或奖励
                                        └─ 等待广告加载（识别 正在处理.png）→ 广告加载完成（inverse）→ 奖励已发放
                                        └─ 奖励已发放 → 返回公会（终点）
                                        └─ 等待广告（识别 跳过.png / Googleplay.png）
```

关键节点：

- **点击广告**：`And`（图片 FeatureMatch `治愈药水.png` + OCR `治愈药水`），`box_index: 1`（取 OCR 文字框中心点击），`roi: [337,490,599,186]`，已实测正常命中（OCR 分数 0.945，框 `[428,658,74,17]`）。
- **等待广告或奖励**：`next: [等待广告加载, 奖励已发放, 等待广告]`，`on_error: 点击广告`，`timeout: 70000`，`rate_limit: 1000`。
- **等待广告加载**：TemplateMatch `正在处理.png`，`timeout: 30000`，`on_error: 点击广告`。
- **广告加载完成**：`inverse: true`（识别到"正在处理"= 还没加载完），TemplateMatch `正在处理.png`，`next: [奖励已发放, 等待广告]`，`on_error: 点击广告`，`timeout: 40000`。
- **奖励已发放**：`And`（图片 FeatureMatch `奖励已发放.png` + OCR `["奖励已发放", "奖励.{0,2}发放"]`，两者 `roi` 均为 `[1075,2,205,102]`），动作 ClickKey(4)，`next/on_error: 返回公会`，`timeout: 40000`。
- **返回公会**：ClickKey(4)，**当前是终点节点（next 为空）**。用户曾表示"返回公会之后想继续跑别的流程"，若后续要接其他流程，需要给它加 `next`。

### 4.2 制造流程（本次任务的操作对象，尚未加数量识别）

```
返回（ClickKey 4）→ 寻找迫击炮 → 点击迫击炮 → 制造迫击炮（滑动）→ 开始建造（点 制造.png）→ 寻找武士刀
     → 点击武士刀 → 制造武士刀（滑动）→ 开始建造武士刀（点 开始制造.png）→ 向下滑动 → 寻找木炭
     → 点击木炭 → 制造木炭（滑动）→ 开始建造_副本（点 制造.png）→ 寻找木梁
     → 点击木梁 → 制造木梁（滑动）→ 开始建造_（点 制造.png）→（终点）
```

各节点现状：

| 节点 | 识别 | 动作 | next |
| --- | --- | --- | --- |
| 寻找迫击炮 | TemplateMatch `工作室.png` | 无 | 点击迫击炮；on_error: 寻找武士刀 |
| 点击迫击炮 | TemplateMatch `迫击炮 独立.png` | Click | 制造迫击炮 |
| 制造迫击炮 | TemplateMatch `迫击炮.png` | Swipe | 开始建造 |
| 开始建造 | TemplateMatch `制造.png` | Click | 寻找武士刀 |
| 寻找武士刀 | TemplateMatch `铁匠铺.png` | 无 | 点击武士刀；on_error: [JumpBack]向下滑动 |
| 点击武士刀 | TemplateMatch `小武士刀.png` | Click | 制造武士刀 |
| 制造武士刀 | TemplateMatch `制造武士刀.png` | Swipe | 开始建造武士刀 |
| 开始建造武士刀 | TemplateMatch `开始制造.png` | Click target `[648,490,199,57]` | 向下滑动 |
| 向下滑动 | 无 | Swipe | 寻找木炭 |
| 寻找木炭 | TemplateMatch `炼金术士的小屋.png` | 无 | 点击木炭 |
| 点击木炭 | TemplateMatch `木炭.png` | Click | 制造木炭 |
| 制造木炭 | TemplateMatch `制造木炭.png` | Swipe | 开始建造_副本 |
| 开始建造_副本 | TemplateMatch `制造.png` | Click | 寻找木梁 |
| 寻找木梁 | TemplateMatch `伐木场.png` | 无 | 点击木梁；on_error: [JumpBack]寻找木炭 |
| 点击木梁 | TemplateMatch `木梁小.png`（FeatureMatch SIFT） | Click | 制造木梁 |
| 制造木梁 | TemplateMatch `木梁.png` | Swipe | 开始建造_ |
| 开始建造_ | TemplateMatch `制造.png` | Click | （终点） |

## 五、当前任务：制造节点数量识别

### 5.1 需求原文

> 在制造迫击炮、武士刀、木炭这类节点时加上数量识别，比如 (66/73)、(199/368)、(48/407) 这类。如果达到上限了或者原材料不足了，就暂停制造，去制造下一个产品。

### 5.2 已确认的设计前提

1. **每个产品制造一次后就去制造下一个产品**（无"连续制造到满"的循环需求）。
2. 数量文字格式：`(左数字/右数字)`，如 `(48/407)`。
3. 用户给出的规则：**右面的数字 ≤ 左面的数字 → 暂停制造、跳过该节点、去下一个产品**。
4. 跳过当前制造窗口前需要先关闭窗口（ClickKey 返回键），否则后续"寻找武士刀"等节点识别不到地图建筑。

### 5.3 关键结论（重要）

**MaaFramework 纯 pipeline 的 OCR 只能做正则匹配，不能做数字大小比较。**

- 纯 pipeline 只能判断"两数相等"（即达到上限，如 `(66/66)`）：利用正则反向引用 `\(?(\d+)/\1\)?`，命中条件是两个数字逐位相同。
- 判断"右<左"（严格小于，如 `(48/40)`）**必须用 Python 自定义识别器（CustomRecognition）**，纯 pipeline 做不到。
- 因此方案 A 与方案 B 的选择取决于：游戏画面中是否会出现"右面严格小于左面"的情况（见第六节待确认问题）。

### 5.4 方案 A：纯 pipeline（只判"两数相等"= 造满）

适用于"只有达到上限（右==左）才跳过"的场景。以迫击炮为例，改造后的结构：

```
制造迫击炮（滑动）→ 检查迫击炮数量
                      ├─ 迫击炮已达上限（OCR 反向引用）→ 关闭制造窗口 → 寻找武士刀
                      ├─ 开始建造（点 制造.png，保持原样）→ 寻找武士刀
                      └─ 两个都不命中且超时 → on_error: 关闭制造窗口 → 寻找武士刀
```

需要新增/修改的节点：

```json
"检查迫击炮数量": {
    "next": ["迫击炮已达上限", "开始建造"],
    "on_error": ["关闭制造窗口"],
    "timeout": 3000,
    "rate_limit": 500
},
"迫击炮已达上限": {
    "recognition": {
        "type": "OCR",
        "param": {
            "expected": "\\(?(\\d+)/\\1\\)?",
            "roi": [/* 数量文字区域，需在 MPE 里框选 */],
            "only_rec": true
        }
    },
    "next": ["关闭制造窗口"]
},
"关闭制造窗口": {
    "action": { "type": "ClickKey", "param": { "key": [4] } },
    "next": ["寻找武士刀"]
}
```

同时把"制造迫击炮"的 `next` 从 `开始建造` 改为 `检查迫击炮数量`。武士刀、木炭、木梁同理（各自接到对应的下一个产品节点）。`on_error` 是"材料不足时制造按钮变灰、`制造.png` 匹配不上"的兜底：超时后自动关窗口跳下一个产品。

**局限：** 反向引用要求两个数字被 OCR 读得完全一致；若 OCR 把 `(66/66)` 误读成 `(66/68)` 会漏判。可对 ROI 精确框选、用 `only_rec: true`、必要时用 `replace` 纠常见误读（如 0↔8）。

### 5.5 方案 B：自定义识别器（支持真正的 右≤左）

Python 识别器（参考已安装 maa 包的 API 编写）：

```python
import re
from maa.custom_recognition import CustomRecognition

class 数量达标(CustomRecognition):
    """OCR 到 (左/右)，当 右 <= 左 时命中"""
    def analyze(self, context, argv):
        reco = context.run_recognition("数量文字", argv.image)
        if not reco or not reco.hit:
            return None
        for r in reco.all_results:
            m = re.search(r"\((\d+)/(\d+)\)", getattr(r, "text", ""))
            if m:
                left, right = int(m.group(1)), int(m.group(2))
                if right <= left:
                    return CustomRecognition.AnalyzeResult(
                        box=r.box,
                        detail={"text": r.text, "left": left, "right": right},
                    )
        return None
```

pipeline 中新增 OCR 辅助节点 + Custom 分支节点：

```json
"数量文字": {
    "recognition": {
        "type": "OCR",
        "param": {
            "expected": "\\d+/\\d+",
            "roi": [/* 数量文字区域 */],
            "only_rec": true
        }
    }
},
"迫击炮已达上限": {
    "recognition": {
        "type": "Custom",
        "param": { "custom_recognition": "数量达标" }
    },
    "next": ["关闭制造窗口"]
}
```

驱动脚本注册方式：

```python
from maa.resource import Resource
resource = Resource()
resource.register_custom_recognition("数量达标", 数量达标())
```

相关 API 要点（已核对该环境下的 maa 包源码）：

- `CustomRecognition.analyze(context, argv)` 返回 `CustomRecognition.AnalyzeResult(box, detail)` 表示命中，返回 `None` 表示未命中。
- `argv.image` 为整张截图（BGR），`argv.roi` 为识别区域；可用 `context.run_recognition(节点名, image)` 调用 pipeline 中已有 OCR 节点。
- `RecognitionDetail` 字段：`hit`、`all_results`、`best_result`；OCR 结果对象有 `text` 与 `box` 字段。

**代价：** Custom 节点无法在 MPE 本地桥 / MaaDebugger 中调试，需要自己写 Python 驱动脚本连模拟器运行（脚本尚未编写，属待办项）。

### 5.6 "材料不足"的补充处理思路

纯 pipeline 无法判断"数量是否增加"，只能靠可见信号：

1. 若游戏有"材料不足"类提示文字 → 加一个 OCR 分支节点直接匹配该文字（纯 pipeline 可做，最简单）。
2. 若制造按钮在材料不足时变灰/禁用 → 截图做 TemplateMatch/FeatureMatch；或者利用方案 A 的 `on_error` 兜底（`制造.png` 匹配不上则超时跳走）。
3. 若材料数字变红 → ColorMatch 红色像素检测。

## 六、待确认问题（阻塞点，需用户回答或提供素材）

1. `(48/407)` 中 48 和 407 分别是什么含义？（当前数量/上限？已有/所需？）
2. **会出现"右面严格小于左面"的画面吗**（比如 `(48/40)`）？——这直接决定用方案 A（纯 pipeline，可 MPE 调试）还是方案 B（自定义识别器）。
3. 数量文字的 ROI 坐标：需要用户在 MPE 里框选，或提供一张制造界面截图由 AI 估算坐标。
4. 材料不足时的界面信号是什么？（提示文字？按钮变灰？材料数字变红？）需要一张对应状态的截图。

## 七、推荐下一步

1. 先让用户回答第六节问题 1、2 → 选定方案 A 或 B。
2. 拿到制造界面截图后确定数量文字的 ROI 与材料不足信号。
3. 修改 Pipeline6.json（保留 `$__mpe_code` 元数据）→ 同步 install 副本 → `ConvertFrom-Json` 校验 → 检查 next/on_error 引用。
4. 用 MPE 本地桥导入调试（注意 And 内联对象坑）；若方案 B，则需编写 Python 驱动脚本实机调试。
5. 实机验证：制造到上限/材料不足时正确跳过并切换到下一个产品。

