# MaaKatana 项目交接说明

更新时间：2026 年 8 月 8 日
项目根目录：`F:\JUSTFORFUN\MaaKatana-main`
正式 UI：`F:\JUSTFORFUN\MFAAvalonia-v2.13.0`
模拟器：LDPlayer，ADB serial 为 `emulator-5554`
设备画面：1280 × 720

## 一、项目用途

这是一个基于 MaaFramework 的个人自动化项目，用于自动操作《日本拼图 nonogram katana》中的游戏流程。

当前主要自动化内容：

- 启动游戏
- 进入公会并观看广告领取奖励
- 进入事务处
- 尝试全部收集
- 依次处理迫击炮、武士刀、木炭、木梁
- 当前已扩展钢铁制造流程
- 制造前使用 OCR 判断数量，避免超过目标数量

## 二、重要目录

### 源项目

- `assets/interface.json`：Maa UI 入口、任务名称、Agent 启动配置。
- `assets/resource/pipeline/`：Pipeline 源文件。
- `assets/resource/image/`：图片识别模板。
- `agent/`：Python 自定义 Agent、数量判断和自定义动作。
- `install/`：项目安装/运行副本。
- `deps/tools/pipeline.schema.json`：Pipeline Schema。
- `任务交接文档.md`：之前已有的项目交接文档，本说明是补充文档。

### 正式 UI

`F:\JUSTFORFUN\MFAAvalonia-v2.13.0`

正式 UI 使用：

- `interface.json`
- `resource/pipeline/`
- `resource/image/`
- `agent/`

修改源项目后，通常需要同步到正式 UI，否则 UI 运行的仍可能是旧配置。

## 三、UI 入口

当前 `interface.json` 中的任务入口：

| UI 任务名称 | Pipeline 入口节点 |
|---|---|
| 启动游戏 | `启动游戏` |
| 观看广告 | `进入公会` |
| 收集产物 | `事务处` |

Agent 配置：

```json
"agent": {
  "child_exec": "python",
  "child_args": ["./agent/main.py"]
}
```

控制器配置使用：

```json
"display_short_side": 720
```

这与 1280 × 720 的游戏画面及现有 ROI 坐标一致。

## 四、当前 Pipeline 文件

### Pipeline6

文件位置：

- `assets/resource/pipeline/Pipeline6.json`
- `install/resource/pipeline/Pipeline6.json`
- 正式 UI：`resource/pipeline/Pipeline6.json`

Pipeline6 是之前的制造流程版本，包含迫击炮、武士刀、木炭、木梁以及建筑检查逻辑。

### Pipeline7

文件位置：

- `assets/resource/pipeline/pipeline7.json`
- `install/resource/pipeline/Pipeline7.json`
- 正式 UI：`resource/pipeline/Pipeline7.json`

Windows 文件系统不区分 `pipeline7.json` 和 `Pipeline7.json` 的大小写，它们实际是同一个源文件。Pipeline7 的编辑器元数据已改为：

```text
filename: Pipeline7
```

当前 Pipeline7 已加入钢铁流程。

## 五、当前制造主流程

制造流程的核心顺序为：

```text
事务处
-> 全部收集（没有按钮时走兜底）
-> 寻找迫击炮
-> 点击迫击炮
-> 迫击炮数量路由
-> 制造迫击炮或跳过
-> 检查武士刀建筑
-> 点击武士刀
-> 制造武士刀
-> 武士刀数量路由
-> 向上滑动
-> 检查木炭建筑
-> 点击木炭
-> 制造木炭
-> 木炭数量路由
-> 检查木梁建筑
-> 点击木梁
-> 制造木梁
-> 木梁数量路由
-> 检查钢铁建筑
-> 点击钢铁
-> 制造钢铁
-> 钢铁数量路由
-> 开始建造钢铁
```

### 全部收集兜底

```json
"事务处": {
  "next": ["全部收集", "寻找迫击炮"]
}
```

```json
"全部收集": {
  "next": ["返回"],
  "on_error": ["寻找迫击炮"]
}
```

如果当前画面没有“全部收集”按钮，会跳过该节点，直接寻找迫击炮。

## 六、建筑检查逻辑

由于 MaaFramework 的普通 Pipeline `on_error` 在自定义数量路由通过 `override_next` 后不能稳定接管，所以制造建筑检查改用了 Python 自定义动作：

```text
检查建筑并跳转
```

自定义参数示例：

```json
{
  "template": ["熔炉.png"],
  "threshold": 0.7,
  "hit_node": "点击钢铁",
  "miss_node": "向上滑动寻找钢铁"
}
```

动作行为：

1. 截图。
2. 直接使用 `TemplateMatch` 检查指定建筑图片。
3. 识别到建筑时通过 `context.override_next` 跳转到 `hit_node`。
4. 未识别到建筑时跳转到 `miss_node`。
5. 识别失败或异常时也安全跳转到 `miss_node`。

当前建筑检查节点：

| 检查节点 | 图片 | 找到后 | 找不到后 |
|---|---|---|---|
| `检查武士刀建筑` | `铁匠铺.png` | `点击武士刀` | `向上滑动` |
| `检查木炭建筑` | `炼金术士的小屋.png` | `点击木炭` | `向上滑动` |
| `检查木梁建筑` | `伐木场.png` | `点击木梁` | `向上滑动寻找木梁` |
| `检查钢铁建筑` | `熔炉.png` | `点击钢铁` | `向上滑动寻找钢铁` |

这些向上滑动节点会继续回到对应的检查节点，形成：

```text
检查目标
-> 找不到
-> 向上滑动 100 像素
-> 再检查目标
-> 找不到继续向上滑动
```

当前向上滑动坐标：

```json
"begin": [620, 380, 30, 30],
"end": [[620, 280, 30, 30]]
```

垂直距离为 100 像素。

## 七、动态点击逻辑

自定义动作：

```text
点击识别框中心
```

文件：`agent/recognition_click.py`

它会读取当前识别结果框 `argv.box`，点击识别框中心，而不是使用固定坐标。

当前使用该动作的节点包括：

- `点击迫击炮`
- `点击武士刀`
- `点击木炭`
- `点击木梁`
- `点击钢铁`

木梁和钢铁等产品会因为前面产品完成后位置变化，因此不应恢复为固定坐标点击。

### 点击钢铁当前配置

`点击钢铁` 已从 `TemplateMatch` 改为 `FeatureMatch`：

```json
"recognition": {
  "type": "FeatureMatch",
  "param": {
    "detector": "SIFT",
    "template": ["点击钢铁.png"],
    "count": 4,
    "ratio": 0.6
  }
}
```

动作仍为：

```json
{
  "type": "Custom",
  "param": {
    "custom_action": "点击识别框中心"
  }
}
```

## 八、数量 OCR 路由

自定义动作：

```text
制造数量路由
```

主要文件：

- `agent/quantity_router.py`
- `agent/quantity_reco.py`
- 安装副本和正式 UI 中也有对应文件。

OCR ROI：

```text
[384, 109, 515, 515]
```

支持类似文本：

```text
x8 (66 / 73)
×8 66/73
48/40
```

判断规则：

```python
skip = current >= limit or current + extra > limit
```

例如：

```text
当前 66，额外增加 8，上限 73
66 + 8 > 73
=> 关闭制造窗口并跳过该产品
```

OCR 识别不到有效数量时会安全关闭当前窗口，并跳到该产品配置的 `next_node`。

## 九、Python Agent 文件

### `agent/main.py`

启动并注册以下模块：

```python
import quantity_reco
import recognition_click
import building_router
```

注意：这些 import 必须位于 `AgentServer.start_up()` 之前，否则自定义动作可能尚未注册。

### `agent/quantity_router.py`

负责读取制造窗口中的数量 OCR，并决定：

- 执行制造按钮。
- 关闭窗口并跳到下一个产品。

### `agent/building_router.py`

注册：

```text
检查建筑并跳转
```

使用直接截图和 `TemplateMatch` 判断建筑是否存在，然后通过 `override_next` 跳转。

### `agent/recognition_click.py`

注册：

```text
点击识别框中心
```

使用当前识别框中心点击目标。

## 十、图片资源

钢铁流程使用：

- `熔炉.png`
- `点击钢铁.png`
- `制造钢铁.png`
- `钢铁.png`

图片应同时存在于：

```text
F:\JUSTFORFUN\MaaKatana-main\assets\resource\image
F:\JUSTFORFUN\MaaKatana-main\install\resource\image
F:\JUSTFORFUN\MFAAvalonia-v2.13.0\resource\image
```

新增图片时，必须检查三个位置是否一致。

## 十一、备份与恢复

### Pipeline6 备份

```text
F:\JUSTFORFUN\MaaKatana-main\Pipeline6.backup-20260808-173110-773.json
```

### Pipeline7 备份

```text
F:\JUSTFORFUN\MaaKatana-main\pipeline7.backup-20260808-175252-411.json
```

恢复 Pipeline7 示例：

```powershell
Copy-Item `
  -LiteralPath 'F:\JUSTFORFUN\MaaKatana-main\pipeline7.backup-20260808-175252-411.json' `
  -Destination 'F:\JUSTFORFUN\MaaKatana-main\assets\resource\pipeline\pipeline7.json' `
  -Force
```

恢复后还要同步到：

```text
F:\JUSTFORFUN\MaaKatana-main\install\resource\pipeline\Pipeline7.json
F:\JUSTFORFUN\MFAAvalonia-v2.13.0\resource\pipeline\Pipeline7.json
```

然后重启 `MFAAvalonia.exe`。

## 十二、最近日志结论

### 2026-08-08 10:34 日志

木梁流程仍使用旧链路：

```text
开始建造_副本 -> 寻找木梁
```

因此绕过了木梁建筑检查，导致 `伐木场.png` 识别分数约为 `0.370`，随后超时。

### 2026-08-08 18:11 日志

钢铁流程中：

- `点击钢铁` 的自定义点击动作成功执行。
- 当时 TemplateMatch 选择了错误候选框 `[355,349,53,51]`。
- 同一截图中存在更高分候选 `[519,574,53,51]`、`[582,574,53,51]`。
- 点击后 `制造钢铁.png` 只得到约 `0.215` 分，说明没有进入正确的钢铁制造窗口。
- 后续已将 `点击钢铁` 改为 `FeatureMatch + SIFT`，需要重新测试验证。

### 2026-08-08 18:18 日志

日志显示：

- 建筑寻找的 `向上滑动` 配置仍是 100 像素。
- 制造节点本身有约 280 到 294 像素的横向滑动。
- `寻找广告` 节点有约 490 像素的纵向滑动。
- 某次 `向上滑动` 在动作开始后被停止，没有出现对应的滑动触摸事件，属于任务停止/动作失败，不等于 100 像素被放大。

## 十三、下一次调试顺序

1. 启动正式 UI。
2. 使用 `收集产物` 入口测试。
3. 重点观察 `点击钢铁` 的 FeatureMatch 日志：
   - 是否出现 `algorithm=FeatureMatch`。
   - 识别框是否位于实际钢铁图标位置。
   - `点击识别框中心` 的点击坐标是否正确。
4. 如果点击后仍无法打开钢铁窗口，保留日志并截图，重点检查：
   - `点击钢铁.png` 是否截取到了真正可点击的钢铁图标。
   - `FeatureMatch` 是否找到多个框。
   - 是否需要改用 `钢铁.png` 或重新截取小图标。
5. 如果出现“大幅滑动”，先从日志中的 `MtouchHelper::touch_down` 坐标判断来源：
   - 垂直约 100 像素：建筑寻找滑动。
   - 横向约 280 像素：制造按钮滑动。
   - 垂直约 490 像素：寻找广告。

## 十四、校验命令

PowerShell JSON 基础校验：

```powershell
Get-Content -LiteralPath 'F:\JUSTFORFUN\MaaKatana-main\assets\resource\pipeline\Pipeline7.json' -Raw | ConvertFrom-Json
```

Python Agent 编译检查：

```powershell
python -m py_compile `
  'F:\JUSTFORFUN\MaaKatana-main\agent\building_router.py' `
  'F:\JUSTFORFUN\MaaKatana-main\agent\quantity_router.py' `
  'F:\JUSTFORFUN\MaaKatana-main\agent\recognition_click.py' `
  'F:\JUSTFORFUN\MaaKatana-main\agent\main.py'
```

Schema 文件：

```text
F:\JUSTFORFUN\MaaKatana-main\deps\tools\pipeline.schema.json
```

## 十五、重要注意事项

- 不要覆盖用户已有的 `my_action.py`，除非明确要求。
- 修改 Pipeline 后必须同步 `assets`、`install` 和正式 UI 三份文件。
- 修改图片后也必须同步三个 `resource/image` 目录。
- 不要只根据节点名称判断是否可以删除节点；要检查普通跳转、自定义参数和组合识别中的字符串引用。
- `on_error` 不一定能接住自定义路由通过 `override_next` 产生的失败分支，因此重要的失败跳转应使用自定义检查动作明确控制。
- 修改后要重启正式 UI，否则界面可能继续使用旧资源哈希。
- 日志导出时间可能晚于实际任务时间，分析时要看日志中真实任务时间和 UI 启动时间。
