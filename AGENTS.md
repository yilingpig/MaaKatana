# MaaKatana 项目交接文档

本文件是当前项目的精简长期交接说明。新任务开始前先阅读，并以实际源码、配置、运行副本和日志为准；不要恢复已删除的入口或过时流程。

## 1. 强制规则

- 未经用户在当前对话明确授权，不得 `git commit`、`git push`、推送标签、创建 Release、强制推送或执行其他发布操作。
- 只修改用户要求的范围；不要清理、提交或覆盖无关的本地工具、调试产物、模板和未跟踪文件。
- 修改后必须自行审阅：检查调用链、Pipeline 引用、Custom 注册、运行副本同步、格式和测试结果。
- 实机成功必须由实际日志或报告证明；离线测试、静态检查和代码推断不能表述为实机成功。
- 涉及点击、滑动、绘制或派遣的改动必须保守验证，不能用无条件重试掩盖状态不确定性。

## 2. Sol / Luna 分工

- **Sol（主代理）**：负责阅读当前文件和日志、梳理项目架构、拆解需求、设计最小可行方案、确定安全边界和验收条件，并审阅 Luna 的代码差异与验证结果。
- **Luna Max（工作代理）**：负责按 Sol 的方案实际修改代码、Pipeline、配置、测试和文档，并执行相应验证。
- 标准顺序：Sol 先分析 → Luna 实现并测试 → Sol 审阅差异、测试和运行证据。
- 如果当前环境无法使用用户指定的 Luna 模型，必须明确说明实际使用的模型与指定模型不一致，不得假称已调用。

## 3. 项目路径与运行副本

- 项目根目录：`F:\JUSTFORFUN\MaaKatana-main`。
- Pipeline 源文件：`assets/resource/pipeline/Pipeline7.json`。
- 源界面配置：`assets/interface.json`。
- MaaFramework Support 实际运行副本：`install/resource/pipeline/Pipeline7.json` 和 `install/interface.json`。
- 主编辑点始终是 `assets/`。修改后同步：
  - `assets/resource/pipeline/Pipeline7.json` → `install/resource/pipeline/Pipeline7.json`
  - `assets/interface.json` → `install/interface.json`
  - `assets/resource/image/` → `install/resource/image/`
- 不要默认同步 `F:\JUSTFORFUN\MFAAvalonia-v2.13.0`；只有确认它是本次实际运行目标时才同步其资源。
- `agent/` 是 Agent 源码目录；与运行副本相关的 Agent 文件修改后，核对 `install/agent/` 是否需要同步，并用哈希确认一致。

## 4. 主流程架构提示

- `agent/main.py` 启动 `AgentServer`，并加载自定义动作/识别模块，包括制造、数量、建筑、奖励、动态滑动、条件路由、黑白数织和彩色数织模块。
- `assets/interface.json` 定义用户可选入口；入口通过 Pipeline 节点进入 `Pipeline7.json`，运行时使用 `install/` 副本。
- 火车拼图流程通过 `Pipeline7.json` 与 `agent/condition_router.py` 的图文条件路由协作：路由根据识别到的图片/OCR 条件调用 `context.override_next` 选择成功或失败分支，再衔接继续、过关确认和返回公会等节点。
- 汽船流程在同一 Pipeline 中包含主界面检查、城市分支、派遣、继续/过关确认和返回路径；不要把独立的彩色数织测试接入火车或汽船主链。
- 制造流程使用数量/建筑路由和自定义 Agent；修改相关节点时必须同时检查 `next`、条件路由参数、Custom 注册和跨节点引用。

## 5. Maa skill 使用原则

- 项目发现、入口和资源结构：使用 `maa-project-init`。
- Pipeline 节点关系、孤立节点和跨文件引用：使用 `maa-pipeline-graph`。
- Pipeline 编写与审阅：使用 `maa-pipeline-guide`。
- Pipeline、OCR、TemplateMatch、CustomAction 和实机流程验证：使用 `maa-pipeline-testing`。
- 可重复的设备发现、截图、识别和命令行执行：使用 `maa-cli-operate`；持续探索式调试优先使用 MaaMCP。
- 任何识别失败先检查候选框、分数、ROI、模板和当前界面，不要盲目降低阈值或增加点击重试。

## 6. 彩色数织当前状态

### 6.1 唯一入口与安全开关

- 当前唯一彩色数织入口是 `彩色数织1080p安全绘制测试`。
- 入口名：`ColorNonogram1080pPaintTest`。
- 相关 Pipeline 节点：`ColorNonogram1080pPaintTest`、`ColorNonogram1080pPaintTestSuccess`、`ColorNonogram1080pPaintTestFailure`。
- `AllowColorNonogramPaintTest` 控制 `allow_paint`；默认 `allow_paint=false`，必须由用户显式开启后才允许实际点击或滑动。
- 已删除并不得恢复：`彩色数织1080p测试`、`彩色数织1080p识别DryRun` 及其只读/DryRun 专用节点。
- 彩色数织是独立测试入口，不进入火车、汽船或其他主链流程。

### 6.2 绘制行为结论

- 先识别并求解 15×15 彩色棋盘，再进行安全清理、试绘、分段绘制和最终读回验证。
- 完成判定必须先通过完整 15×15 棋盘验证；外部 clue 区域自动出现的 X 不计入棋盘单元格验证。
- 单格使用一次 `click`；长度为 2 的段使用两个独立点击；长度为 3 及以上的连续段使用一次 `swipe`。
- 禁止无条件重复绘制；重复点击或滑动可能把已正确填色的格子切换为空白或其他状态。
- 需要擦除时使用底栏的 `抹掉颜色.png`；切回涂色模式时直接点击目标颜色。
- 完整棋盘验证成功后，识别到 `恭喜过关了.png` 或 `继续.png` 即视为完成成功。
- 成功路径删除本次运行登记的临时 PNG，保留 JSON 报告；失败路径保留失败截图和 JSON，便于定位问题。
- 用户已关闭游戏坐标刻度设置；不要恢复或新增坐标刻度专项识别逻辑。

### 6.3 当前仍需优化的颜色方向

以下是后续可由 Luna 实现的方向，目前不要当作已实现功能：

- 对每个调色板按钮采样中心区域，使用多数像素或中位数而不是单点/边缘像素。
- 使用 HSV 或 Lab 等感知色差，并结合匹配面积、按钮位置和候选排序区分相近颜色。
- 点击前确认目标调色板按钮的中心和选中状态，降低左右相近红色误点进入颜色编辑界面的风险。
- 涂色后对局部格子做稳定读回；只有达到稳定预期颜色才继续，且失败时不得无条件重绘。
- 白色、浅灰色等接近背景的颜色应结合格子内部采样、邻域和网格状态判断，不能只依赖单个像素。

## 7. 修改与验证清单

- 修改源文件后同步对应 `install/` 运行副本。
- 解析修改后的 JSON，检查入口、节点名称、`next`、条件路由和资源引用。
- Python 修改至少执行：

```powershell
python -m py_compile agent\<changed_file>.py install\agent\<changed_file>.py
```

- 彩色数织相关修改可执行：

```powershell
$env:PYTHONPATH='agent'
python -m pytest tests\test_color_nonogram_paint_test.py tests\test_color_nonogram_solver.py -q
```

- 项目级检查：

```powershell
.\tools\run_project_checks.ps1
```

- 最终至少执行：

```powershell
git diff --check
git status --short
```

- 报告中区分代码测试、静态检查和实机验证；列出未验证的部分和实际使用模型。

## 8. 项目现状与困境

### 8.1 彩色数织识别当前现状

**功能状态：**
- ✅ 白色或无背景色拼图：识别和绘制功能正常工作
- ✅ 标准 15×15 彩色数织棋盘：Grid 检测、颜色分类、求解和绘制流程稳定
- ✅ 调色板颜色识别：基本颜色识别和选择功能正常
- ❌ 彩色背景拼图：绿色 [6, 213, 6]、橙色 [240, 154, 8]、蓝色等背景色拼图无法自动识别

**核心技术参数（当前稳定配置）：**
- 单元格采样边距（margin）：0.22（22%）
- 采样区域：每个单元格中心 56% × 56% 的区域
- 背景颜色容差：色差阈值用于区分背景和填充颜色
- 分类逻辑：基于 RGB 色差的简单分类，不包含网格线污染检测

### 8.2 已知技术困境与根本原因

#### 8.2.1 彩色背景识别失败问题

**问题表现：**
- 带彩色背景（绿色、橙色、蓝色等）的拼图在基线验证阶段失败
- 错误信息：`board is not blank before paint`（绘制前棋盘不为空白）
- 识别系统误认为空白棋盘已经被填充

**根本原因：**
- 彩色背景拼图的网格线和装饰性边框带有深色（黑色或深灰色）
- 当前 22% 边距的采样区域仍会采集到部分网格线像素
- 网格线的深色与调色板中的深色（如黑色、深蓝色）产生色差匹配
- 基线验证误判：系统认为这些网格线颜色是已填充的调色板颜色

**已尝试的解决方案及失败原因：**

1. **方案一：增大采样边距至 30%**
   - 结果：仍然失败，网格线仍被采样到
   - 原因：30% 边距不足以完全避开网格线

2. **方案二：增大采样边距至 38% + 网格污染检测**
   - 实施：将边距提高到 38%，采样区域缩小到 24% × 24%
   - 添加 `_detect_border_columns_rows` 函数检测装饰性边框
   - 在 `_classify_cell` 中添加网格线污染检测逻辑
   - 结果：彩色背景问题未解决，且引入新问题
   - 新问题：原本正常的无背景色拼图也无法识别
   - 根本原因：采样区域过小（仅 24% × 24%），填充后的颜色也无法被正确检测到
   - 表现：绘制验证超时，系统认为已绘制的格子仍然是背景色

3. **方案三：完整回退至原始配置**
   - 决策：放弃自动识别彩色背景拼图
   - 行动：将所有修改回退到原始状态（margin = 0.22）
   - 结果：无背景色拼图功能恢复正常
   - 当前状态：保持稳定的原始配置

#### 8.2.2 技术权衡与架构决策

**核心矛盾：**
- 边距太小（如 22%）：网格线污染采样区域，导致彩色背景误判
- 边距太大（如 38%）：采样区域过小，无法可靠检测填充后的颜色
- 中间边距（如 30%）：无法完全解决任何一个问题

**架构决策：**
- 优先保证标准拼图（无背景色）的可靠性和稳定性
- 不追求支持所有类型拼图的通用解决方案
- 对于特殊情况（彩色背景），采用手动预处理 + 自动化的混合策略

**当前策略：**
- 遇到彩色背景拼图时，用户手动将背景涂白后再运行自动识别
- 保持代码简单、稳定、可维护
- 避免过度复杂的检测逻辑导致可靠性下降

### 8.3 其他已知问题

#### 8.3.1 OCR读取失败问题（2026-08-23新发现）

**问题表现：**
- 批量测试中出现"unable to read row clue at line=X slot=Y"错误
- 错误发生在颜色识别**成功之后**，OCR读取数字阶段失败
- 失败案例：
  - `paint_failure_20260823_131518_247938.json`: line=0 slot=6, 4色板, 单元格39.5×39.7px
  - `paint_failure_20260823_131448_389917.json`: line=0 slot=6, 4色板, 单元格41.3×41.4px
  - `paint_failure_20260823_131411_394523.json`: line=0 slot=5, 3色灰板, 单元格37.6×37.7px

**关键代码位置：**
- `agent/color_nonogram_solver.py:675` 抛出错误
- `agent/color_nonogram_solver.py:552-569` `_read_cell()` OCR读取函数
- `agent/color_nonogram_solver.py:555` OCR threshold=0.2（20%置信度）

**诊断过程：**
1. **初步误判**：最初认为是颜色识别容差问题
   - 实施了adaptive tolerance和fallback nearest-color matching
   - 验证：background_rgb正确采样，颜色识别成功
   - 结果：修复无效，问题仍然存在

2. **正确诊断**：实际是OCR引擎读取失败
   - 颜色识别已经成功（`color is None`检查通过）
   - OCR返回空结果或无法解析为数字
   - `_number(texts)`返回None导致异常

**根本原因分析：**
- OCR引擎对某些渲染的数字置信度低于0.2阈值
- 可能影响因素：
  - 数字渲染质量差（抗锯齿、字体细节）
  - 单元格尺寸接近40px边界，不触发放大策略
  - 特定字体或颜色组合对OCR不友好
  - 低对比度clue区域

**已尝试的修复方案：**

1. **方案一：放宽OCR阈值重试**（2026-08-23）
   - 实施：在`_read_cell()`中添加threshold=0.1的重试逻辑
   - 位置：`agent/color_nonogram_solver.py:552-586`
   - 策略：常规OCR失败后立即用更低阈值重试
   - 结果：用户反馈"依旧失败"

**当前状态：**
- 问题未解决，OCR读取失败仍然是批量测试的主要障碍
- 已有adaptive color tolerance修复（虽然不是主要问题）
- 已有OCR threshold降低重试机制（但仍不足以解决问题）

**后续可探索方向（未实施）：**
- 调整`use_enlarged_ocr`阈值（当前40px），覆盖39-41px边界案例
- 增加OCR预处理：对比度增强、二值化、去噪
- 多次OCR重试取最可信结果
- 改进模板匹配`_template_digit_from_patch()`的置信度阈值
- 针对小数字优化ROI计算（当前0.1边距，0.8宽高）
- 记录OCR失败时的实际patch图像，用于离线分析

**交接建议：**
- OCR失败是识别链路中的真实瓶颈，不是伪问题
- 不要再从颜色识别角度尝试修复（已排除）
- 需要系统性分析OCR失败的patch图像特征
- 可能需要调整MaaFramework OCR配置或切换OCR引擎

#### 8.3.2 求解器限制
- 部分拼图报告 `no valid solution`（无有效解）
- 原因：求解器算法限制，而非识别问题
- 状态：已知限制，暂不处理

#### 8.3.3 绘制稳定性检测超时
- 偶发现象：`paint segment did not reach a stable expected color before timeout`
- 表现：绘制动作执行后，颜色验证超时
- 可能原因：
  - 游戏界面动画延迟
  - 颜色填充渲染时间过长
  - 稳定性检测阈值设置问题
- 状态：偶发问题，未系统性解决

#### 8.3.4 相近颜色误识别风险
- 调色板中存在多个相近的红色、粉色等颜色
- 当前基于单点或小区域 RGB 采样
- 风险：可能误点相邻颜色，进入颜色编辑界面
- 状态：已识别风险，待优化（见 6.3 节）

### 8.4 总结与展望

**当前可用性：**
- 对于标准的白色背景或无背景色 15×15 彩色数织拼图，系统可以稳定识别、求解和绘制
- 已验证的工作流程完整，包括 Grid 检测、颜色提取、约束求解、分段绘制和验证

**核心限制：**
- 无法自动处理带彩色背景的拼图，需要手动预处理
- 网格线污染问题在当前架构下无通用解决方案

**工程哲学：**
- 优先保证核心场景的可靠性，而不是追求覆盖所有边缘情况
- 对于特殊情况，采用人机协作的混合策略
- 保持代码简洁、可维护，避免过度工程化

## 9. 新任务启动顺序

1. 阅读本文件，执行 `git status --short`，识别用户已有改动和无关未跟踪文件。
2. 查阅当前 `assets`、`install` 入口配置、Pipeline、Agent 注册和最新相关日志。
3. 先由 Sol 给出最小改动方案与验收条件，再由 Luna 实施；完成后由 Sol 审阅。
4. 修改后同步运行副本，执行针对性测试和 `git diff --check`。
5. 未获用户本轮明确授权时，不进行任何 Git 提交或发布操作。

## 10. 2026-08-23 彩色数织阶段性交接记录

### 10.1 本轮工作目标

本轮工作只围绕独立入口 `ColorNonogram1080pPaintTest` 展开，目标是定位并处理以下问题：

1. 数字 `1` 密集时，OCR 可能与 `7` 混淆，导致候选线索错误或求解无解。
2. 求解完成前长时间没有绘制，调试端看起来像卡死。
3. 任务可能仍在后台执行，但终端没有阶段进度，用户因此提前停止调试。
4. 对原本可解的拼图，候选恢复等待过短导致被误判为无解。

必须继续遵守：已有成功绘制案例优先；修改要小范围；不得把 OCR 失败、候选无解、求解超时、绘制读回失败和完成态漏检混为一类；未有新的实机报告前不得声称实机问题已解决。

### 10.2 已完成的代码工作

#### A. clue OCR 小尺寸保护

文件：

- `agent/color_nonogram_solver.py`
- `install/agent/color_nonogram_solver.py`

已完成内容：

- 对小 clue 尺寸（约 `<=42px`）增加保护路径，覆盖已知 `37.6px`、`39.5px`、`41.3px` 样本。
- 空白浅蓝 clue 只有在近似均匀时才跳过；真实 glyph 继续走 OCR/template 路径。
- 未调整普通数字的整体 threshold、颜色容差和绘制逻辑。

#### B. `1/7` 候选保留

文件同上。

已完成内容：

- 当 OCR 与模板识别在 `1/7` 之间冲突时，不再立即丢弃另一候选。
- 保留候选交给后续 disambiguation。
- 不改变普通数字路径和已有成功案例的识别参数。

#### C. 候选组合预算和生成超时

文件：

- `agent/color_nonogram_disambiguation.py`
- `install/agent/color_nonogram_disambiguation.py`
- `agent/color_nonogram_core.py`
- `install/agent/color_nonogram_core.py`

已完成内容：

- 单行候选组合增加 `maximum_combinations`，默认 `100000`。
- 增加 `LineCombinationBudgetExceeded`。
- 候选组合生成增加时间限制，默认单行约 `3s`。
- `line_patterns()` 增加 deadline 检查，避免合法模式生成本身无限耗时。
- `find_solutions()` / `solve_puzzle()` 支持 `maximum_seconds`。
- 深度搜索和模式生成都能抛出 `SolutionSearchTimeout`。

#### D. 直接求解保护

文件：

- `agent/color_nonogram_solver.py`
- `install/agent/color_nonogram_solver.py`

已完成内容：

- `_solve_with_candidate_recovery()` 的初次直接求解使用保守 `3s` 上限。
- 直接求解超时后进入原有候选恢复，不直接改变普通可解路径。
- 源文件和运行副本已同步。

#### E. 单候选求解超时处理

文件：

- `agent/color_nonogram_disambiguation.py`
- `install/agent/color_nonogram_disambiguation.py`
- `tests/test_color_nonogram_disambiguation.py`

当前参数：

- 全局候选恢复：`maximum_seconds=12.0`
- 单候选求解默认：`DEFAULT_MAXIMUM_CANDIDATE_SECONDS = 3.0`
- 单候选超时：捕获 `SolutionSearchTimeout`，计入 `candidate_timeouts`，继续尝试其他候选。

结果统计包含：

- `attempts`
- `candidate_timeouts`
- `row_option_counts`
- `column_option_counts`

这些统计会拼入 `DisambiguationResult.reason`，并通过现有 `paint_test` 失败处理进入失败 JSON。

#### F. 求解前中间报告

文件：

- `agent/color_nonogram_paint_test.py`
- `install/agent/color_nonogram_paint_test.py`

已完成内容：

- 在 `before_solve` 前先写入 `paint_before_*.json`。
- 求解异常时可保留失败截图和失败报告，不再完全没有产物。
- 现有失败处理仍通过 `context.override_next` 跳转到失败节点。

### 10.3 关键实机证据

#### A. 曾被误认为卡死、实际最终成功的样本

文件：

- 日志：`install/debug/maafw.log`
- 成功报告：`debug/color_nonogram_live/paint_after_20260823_164818_421081.json`

时间线：

- `2026-08-23 16:28:25`：任务启动。
- `2026-08-23 16:28:45`：最后一批 OCR 日志。
- `2026-08-23 16:45:29`：生成试绘截图。
- `2026-08-23 16:48:18`：生成完成前后的绘制验证截图。
- `2026-08-23 16:48:23`：`Node.Action.Succeeded`、`Tasker.Task.Succeeded`。

报告确认：

- `status=success`
- `phase=complete`
- `expected_segments=120`
- `painted_cells=120`
- `completion.detected=true`

结论：这次不是绘制点击卡死，而是候选求解/恢复耗时很长且期间没有进度反馈；从最后 OCR 到试绘约 17 分钟。

#### B. 2026-08-23 17:10/17:13 的两次超时样本

文件：

- `debug/color_nonogram_live/paint_failure_20260823_171147_590193.json`
- `debug/color_nonogram_live/paint_failure_20260823_171448_226825.json`

共同特征：

- `phase=before_solve`
- `reason=solution search exceeded timeout=11.999s`
- 没有 `trial_screenshot`
- 没有 `paint_operations`
- `painted_cells=0`

日志中的 OCR 以数字 `1` 为主，同时出现数字 `7`，支持 `1/7` 歧义导致搜索困难的判断。

结论：当时不是 `line_patterns` 生成超时，而是候选拼图内部的 `solve_puzzle(require_unique=True)` 深搜占满了全局预算。

#### C. 2026-08-23 17:53、17:55、17:57 的三次新实机失败

文件：

- `debug/color_nonogram_live/paint_failure_20260823_175414_946073.json`
- `debug/color_nonogram_live/paint_failure_20260823_175702_104036.json`
- `debug/color_nonogram_live/paint_failure_20260823_175858_792547.json`

共同特征：

- `phase=before_solve`
- `status=failure`
- 无试绘、无绘制动作。

其中两次明确出现：

```text
attempts=1; candidate_timeouts=1;
row_option_counts=(1, 1, ..., 1);
column_option_counts=(1, 1, ..., 1)
```

这说明候选恢复实际上只有一个候选，程序在旧的 `0.75s` 单候选预算内没有完成唯一解搜索，就将其记为超时并最终报告 `no_solution`。这不能证明拼图真的无解，也不能简单归因于 OCR 重读已经把线索改坏。

当前已将单候选默认等待提高到 `3.0s`，全局仍限制为 `12s`；上述三次实机是在 `0.75s` 默认值时运行的，不能用来评价 `3.0s` 修复后的实机效果。

### 10.4 当前真实现状

已确认：

- 标准白色/无背景 15×15 彩色数织的识别、求解和绘制主链仍存在成功案例。
- 绘制动作本身在成功报告中可以完成 120 个格子并通过完整棋盘验证。
- 长时间未绘制的主要瓶颈在 `before_solve` 的候选恢复，而不是 palette 点击、click/swipe 或颜色稳定读回。
- `1` 密集、`1/7` 混淆会放大候选恢复难度。
- 先前“无失败报告”的问题已经改善：现在失败路径通常会生成 `paint_failure_*.json` 和失败截图。

仍未确认：

- `3.0s` 单候选预算能否让之前三类样本正常进入试绘。
- 重读 OCR 是否在部分样本中确实改坏了原本可解的线索。
- 单候选超时后继续候选的策略是否能在真实 `1/7` 样本中找到正确候选。
- 是否需要把唯一候选与多候选使用不同的预算；目前尚未增加第二层更长重试。
- 调试终端的阶段心跳尚未完成，日志仍可能在长求解期间缺少可见进度。

### 10.5 当前主要困境

1. **唯一候选可能仍然难解**：即使 `row_option_counts` 和 `column_option_counts` 都为 1，`require_unique=True` 仍需要确认没有第二个解，搜索可能比找出一个解更慢。
2. **候选搜索与 OCR 重读耦合**：初次 clue 读取失败后会针对颜色重新读取线索；重读结果可能与初次结果不同，需要保留两套线索进行离线对比。
3. **失败原因字符串仍有重复统计**：当前 `paint_test` 外层又包装了一次 disambiguation reason，可能出现两次 `attempts=...; candidate_timeouts=...`；不影响功能，但降低可读性，后续可清理。
4. **缺少求解阶段心跳**：实际耗时可能很长，用户容易误以为卡死而手动停止；后续应增加低频日志，不改变算法。
5. **彩色背景仍明确不支持**：不要把本轮求解超时工作扩展到彩色背景网格污染问题。

### 10.6 下一次新对话建议顺序

1. 先查看 `2026-08-23 17:53–17:59` 三次样本的最新报告，确认是否已使用 `DEFAULT_MAXIMUM_CANDIDATE_SECONDS=3.0`。
2. 重新实机测试：
   - 一幅之前成功绘制的普通拼图；
   - 一幅 `1` 密集、之前超时的拼图；
   - 一幅存在明确 `1/7` 候选的拼图。
3. 重点记录：
   - `phase`
   - `reason`
   - `attempts`
   - `candidate_timeouts`
   - `row_option_counts`
   - `column_option_counts`
   - 是否生成 `trial_screenshot`
   - 日志中的 `post_click` / `post_swipe` 首次出现时间。
4. 若仍是唯一候选超时，不要继续盲目增加全局时间；先离线比较初次 clue、重读 clue、模板候选和颜色总数，确认是否为错误 OCR 或 `require_unique` 过严。
5. 若求解成功但终端长时间无输出，再只增加阶段心跳和低频绘制进度日志。
6. 若确认重读 OCR 改坏线索，再设计“保留初次可解结果、重读结果仅作候选”的回退策略；不得无条件选择重读结果。

### 10.7 当前验证记录

最近一次代码层验证（2026-08-23）：

- 彩色数织相关测试：`82 passed, 7 skipped, 13 subtests passed`
- `py_compile`：源文件和 `install/agent` 运行副本通过。
- 源文件与运行副本 SHA256 已核对一致。
- `git diff --check`：通过。
- 未执行 Git commit/push。
- `3.0s` 单候选预算尚未取得新的实机成功证据。

本节记录的是交接状态，不代表所有问题已经解决；新对话必须以最新实际报告和日志为准。

## 11. 2026-08-29 广告奖励流程与商店流程交接

### 11.1 广告奖励目标流程

```text
Google Play → 返回键 → 奖励已发放识别 → 奖励内容确认 → 返回
```

Google Play 未出现时，奖励识别成功后也必须先返回，再进入奖励内容确认，确认成功后才能返回公会。Google Play 出现但奖励文字识别失败时，返回后仍必须进入奖励内容确认，不得直接绕过确认。

- 奖励内容确认允许任一图片或 OCR 字段命中：`+1治愈药水感谢您.png`、`治愈药水019.png`、`治愈药水020.png`、`治愈药水`。
- 奖励图片 ROI 为 `[337, 490, 599, 186]`；顶部奖励状态 ROI 为 `[1075, 2, 205, 102]`。
- `奖励已发放` 成功后执行 `ClickKey key=4`，再进入 `广告奖励确认`；确认失败不得无条件进入 `返回公会`。

### 11.2 奖励已发放识别修复

最新失败日志表明图片和 OCR 均已识别到奖励文字，但嵌套 `And` 因倒计时排除节点返回空结果而失败。根因是引用节点上的 `inverse=true` 没有在该嵌套组合中产生预期语义，而不是 OCR 本身未命中。

当前实现：

- `agent/reward_reco.py` 注册 `奖励倒计时有效确认` 与 `奖励倒计时不存在确认`。
- 倒计时模板最高分低于 `0.7` 时，“不存在确认”直接返回成功；达到 `0.7` 时返回失败。
- Pipeline 节点 `奖励倒计时未出现` 的 `custom_recognition` 必须指向 `奖励倒计时不存在确认`，不能恢复旧的 `inverse=true` 实现。
- `奖励已发放图片确认排除倒计时` 与 `奖励已发放文字确认排除倒计时` 要求奖励条件和倒计时不存在条件同时成立。
- OCR 允许精确匹配 `奖励已发放`，并可接受末尾紧邻的 `X/x/×` 关闭符号；不得把“奖励已发放关闭”“奖励已发放领取”等额外文本当成目标。

涉及文件：`agent/reward_reco.py`、`install/agent/reward_reco.py`、`assets/resource/pipeline/Pipeline7.json`、`install/resource/pipeline/Pipeline7.json`、`tests/test_reward_reco.py`。

### 11.3 广告等待循环问题及当前处理

旧链路中 `等待广告或奖励`、`等待广告加载`、`广告加载完成` 的 `on_error` 都指向 `点击广告`。当 `正在处理.png` 阶段性无法命中时，会重新点击广告，形成约 25 轮后回到点击流程的循环。

当前链路为：

```text
等待广告或奖励失败 → 广告等待失败终止
等待广告加载失败   → 广告等待失败终止
广告加载完成失败   → 广告等待失败终止
```

- `广告等待失败终止` 使用 `DoNothing`，不再无条件回点，也不引入无限循环。
- `点击广告` 仍要求图片和文字条件同时满足。
- 当前取舍是识别状态不确定时终止任务，可能需要用户重新启动；这比误点击和重复触发广告更安全。
- 后续若要自动恢复，只能增加有上限且带状态确认的重试：确认仍在公会广告入口、治愈药水图片仍存在、未进入广告或奖励页；不得恢复无条件 `on_error → 点击广告`。

### 11.4 广告加载失败检测计划（尚未接入主流程）

独立测试入口的计划流程：

```text
正在处理出现 → 正在处理消失 → OCR 检测“加载失败”
→ 治愈药水图片仍存在时重新点击 → 重新等待加载
```

- 暂无可靠“加载失败”图片模板，暂时使用 OCR。
- 重点击必须限制次数，建议最多 1～2 次，超过后进入明确失败节点。
- 必须同时确认“加载失败”文字、广告入口状态和治愈药水图片存在。
- 未完成独立测试前不得接入广告主流程。

### 11.5 商店购买流程

当前只购买武士刀和铁砂：

```text
公会 → 商店 → 武士刀 → 滑条向右到底 → OCR+购买按钮图片确认 → 购买
     → 铁砂 → 滑条向右到底 → OCR+购买按钮图片确认 → 购买 → 返回公会
```

- 示例资源为 `武士刀商店.png`、`铁砂.png`。
- 购买按钮必须同时满足 OCR `购买` 与 `购买按钮.png`，防止误点 `取消`。
- 滑动参照收集产物流程，不得无条件重复滑动或购买。
- 新增商品前先确认商品图片、滑条、按钮和返回路径。

### 11.6 MPE、MaaFramework Support 与运行副本

- 相同 `1920×1080` 截图在 MPE 和 Support 中仍可能因运行副本、资源根目录、Agent 进程、截图归一化、Pipeline 合并状态、ROI 或输入方式不同而结果不同。
- Support 实际运行 `install/`，不是直接运行 `assets/`；修改后同步 Pipeline、界面、图片和 Agent 运行副本。
- Support 相关设置曾按 720 短边归一化调整；不要只凭 MPE 成功推断 Support 成功。
- 识别问题必须查看 Support 日志中的候选框、分数和当前截图；离线测试不能表述为实机通过。

### 11.7 当前验证、未验证和后续计划

已完成代码级验证：

- `tests/test_reward_reco.py`：`6 passed, 8 subtests passed`。
- `py_compile` 通过；两份 Pipeline JSON 解析和节点引用检查通过。
- `agent/reward_reco.py` 与 `install/agent/reward_reco.py` SHA256 一致。
- `assets/resource/pipeline/Pipeline7.json` 与 `install/resource/pipeline/Pipeline7.json` SHA256 一致。
- `git diff --check` 通过；CRLF/LF 提示只是工作树格式警告。

尚未确认：

- 修复后的奖励识别是否已通过新的 Support 实机运行。
- 真实倒计时是否稳定阻止奖励节点通过，奖励命中后是否始终执行返回并进入奖励确认。
- `广告等待失败终止` 是否满足实际使用需求；目前只确认消除了无条件回点逻辑，未证明可自动恢复加载失败。
- 武士刀和铁砂商店流程尚未完成新的端到端实机验收。

后续顺序：

1. 在 Support 实机验证真实倒计时、奖励已发放、Google Play 返回后三种状态。
2. 检查日志出现奖励识别成功、`ClickKey key=4`、奖励确认开始和成功；确认失败时不得出现返回公会。
3. 验证等待失败的最终节点不会再次进入点击广告；如需恢复，设计有上限的状态确认重试。
4. 独立测试加载失败 OCR 和最多 1～2 次重点击，不接入主流程。
5. 端到端测试武士刀和铁砂购买，重点确认不误点取消、滑条到底、购买后返回正确。
6. 彩色数织继续按第 10 节推进，与广告和商店流程隔离。

本节状态截至 `2026-08-29`；所有实机结论必须由新日志或报告更新。

### 11.8 彩色数织答案页反推与失败分类

彩色数织独立入口在正常线索求解无法及时完成、OCR 未能在规定时间内进入绘制、或识别失败时，计划/现有的兜底方向是查看游戏答案页反推矩阵：

```text
正常识别 → 正常求解 → 绘制
正常求解超时或 OCR 失败 → 点击查看答案 → 截取答案页 → 识别 15×15 色块
→ 按调色板颜色映射 → 返回键 → 执行绘制
```

- 答案按钮资源为 `查看答案.png`；进入答案页后使用设备返回键，再执行绘制。
- 用户已确认调色板中的所有颜色一定会在答案页使用，因此答案识别时应以当前调色板建立颜色映射，不应因为答案颜色种类与调色板统计暂时不一致就直接认定无解。
- `answer=13 palette=7`、`solution color is outside palette at row=0 column=0` 等错误优先按答案截图未生成、答案页状态错误、颜色采样/聚类错误或调色板识别错误排查，不能直接把它们包装为“无解”。
- 答案图矩阵与现有绘制矩阵必须使用同一行列坐标、同一颜色 ID 和同一调色板映射；白色和浅色是合法颜色，不能被当作背景无条件过滤。
- 答案页反推必须有固定 15×15 网格确认、颜色数量检查、调色板成员检查和失败报告；不得在矩阵未确认时点击或滑动。
- 答案页截图应保存到失败报告目录，失败时保留截图和 JSON，成功后可清理本次运行的临时图片。
- 当前已知限制：彩色背景棋盘仍可能被装饰性网格线污染；优先保证标准白色背景/无背景色拼图，特殊彩色背景暂采用人工预处理。

失败原因必须区分：

- `before_grid`：棋盘定位或完整 15×15 网格确认失败。
- `before_solve`：线索 OCR、候选组合、求解超时或确实无解；超时不得直接记为无解。
- `answer_fallback`：答案截图、颜色数量、调色板映射或答案矩阵校验失败。
- `paint`：点击/滑动绘制或颜色稳定读回失败。
- `complete`：绘制完成但过关/继续状态漏检。

后续计划：

1. 继续收集完整失败截图、答案截图和失败 JSON，先确认答案页截图是否真实生成。
2. 对答案图和调色板分别记录实际 RGB/HSV/Lab 样本、聚类数量和映射关系，定位“颜色数量不一致”的来源。
3. 将答案矩阵转换为现有绘制器使用的颜色 ID，先做离线矩阵一致性测试，再考虑 Support 实机。
4. 保持 `allow_paint=false` 默认安全开关；未完成网格、颜色和调色板确认时禁止实际绘制。
5. 求解阶段继续增加低频阶段日志，避免用户因长时间无输出而手动停止任务。

## 12. 黑白和彩色数织从 1080p 迁移到 720p 的方案

### 12.1 迁移目标

黑白数织和彩色数织后续需要接入主流程，因此最终应统一在 MaaFramework Support 的 720p 显示配置下调试和运行。但迁移目标不是重写求解器，而是将“截图、ROI、模板和输入坐标”从固定 1080p 适配为运行时分辨率，同时保留既有 1080p 结果作为回归基线。

目标分层：

```text
主流程 / Pipeline
        ↓
720p 识别与坐标适配层
        ↓
黑白和彩色数织核心求解器
        ↓
720p 点击、滑动和绘制验证
```

必须保留：

- 黑白和彩色数织的 10×10、15×15、20×20 求解逻辑；
- 彩色数织的颜色 ID、调色板映射、答案页矩阵和绘制分段逻辑；
- 失败报告、截图、答案反推和 `allow_paint=false` 安全开关；
- 已验证的 1080p 截图、模板、ROI、报告和成功案例，作为迁移前后对照。

### 12.2 分辨率和坐标契约

当前 Support 配置为 `display_short_side: 720`。对于横向 `1920×1080` 设备，通常对应 `1280×720` 的归一化截图，但必须以 `post_screencap()` 的实际尺寸为准，不得仅凭配置推断。

逻辑坐标与运行坐标必须分离：

```text
逻辑坐标空间：固定使用 1920×1080 作为历史基准
运行坐标空间：使用当前截图实际宽高，例如 1280×720
```

统一缩放关系：

```python
scale_x = actual_width / 1920.0
scale_y = actual_height / 1080.0
```

所有固定点和 ROI 通过统一适配函数转换：

```python
def scale_point(x, y, actual_width, actual_height):
    return (
        round(x * actual_width / 1920),
        round(y * actual_height / 1080),
    )


def scale_roi(x, y, width, height, actual_width, actual_height):
    return (
        round(x * actual_width / 1920),
        round(y * actual_height / 1080),
        round(width * actual_width / 1920),
        round(height * actual_height / 1080),
    )
```

注意：截图坐标和 `post_click`/`post_swipe` 输入坐标是否属于同一坐标空间必须通过实机校准确认。若截图为 720p、输入仍要求原始坐标，则需要在输入前执行反向转换，不能凭经验假设。

### 12.3 CustomAction 迁移原则

CustomAction 应优先根据实际截图动态工作，不应继续硬编码“必须是 1920×1080”。

黑白数织：

- 使用实际截图执行 `detect_grid(image)`；
- 根据实际网格线计算 `geometry.center()`；
- OCR ROI 随实际截图缩放；
- 点击和滑动使用运行时网格中心；
- 完成验证使用当前截图，不依赖固定 1080p 格子坐标。

彩色数织：

- 保留 `GridGeometry`、15×15 矩阵和颜色 ID；
- `_capture()` 不再把 `[1920, 1080]` 作为唯一合法截图尺寸；
- 棋盘、调色板、答案页和完成状态 ROI 统一走分辨率适配；
- 单元格颜色采样、调色板按钮点击、绘制点击、滑动和最终读回均使用运行坐标；
- 白色、浅色和彩色背景的识别语义不能因缩放而被无条件过滤；
- 答案页 15×15 矩阵与正常求解矩阵必须共享同一颜色 ID 和坐标顺序。

当前彩色入口中的 `expected_resolution: [1920, 1080]` 是历史 1080p 基线约束。迁移到 720p 时应改为“记录实际分辨率并进行适配”，不能简单把期望值改成 `[1280, 720]` 后继续使用未缩放的 ROI 和点击坐标。

### 12.4 Pipeline 和模板资源迁移

CustomAction 可以动态缩放坐标，但 Pipeline 的普通识别节点、固定 ROI 和模板不会自动等价适配。因此需要为主流程准备 720p 专用资源，不能直接覆盖已经验证的 1080p 资源。

推荐资源组织：

```text
assets/resource/image/1080p/
assets/resource/image/720p/
```

如果现有图片仍在 `assets/resource/image/` 根目录，应将其视为当前 1080p 基线，新增 720p 子目录或明确的 720p 命名，不要破坏历史引用。

优先转换和重新截取主流程实际需要的资源：

1. 进入黑白数织页面的模板；
2. 进入彩色数织页面的模板；
3. `查看答案.png`；
4. `继续.png`、过关和失败状态图片；
5. 返回和异常状态图片；
6. 调色板及购买/操作按钮相关模板；
7. 其他固定 ROI 对应的图片。

模板迁移要求：

- 720p 模板优先从 720p 实机截图重新裁取；仅在界面稳定且比例确认一致时才使用缩放生成；
- Pipeline 中的 720p ROI 必须与 720p 模板位于同一坐标空间；
- 1080p 和 720p 模板不能在同一节点中无条件混用；
- 修改 `assets/` 后同步 `install/`，并核对 Pipeline、界面、图片和 Agent 运行副本；
- 720p 迁移完成前，不删除或覆盖 1080p 基线资源。

### 12.5 分阶段迁移顺序

1. **冻结 1080p 基线**：保留现有成功截图、失败报告、模板、入口和测试结果。
2. **确认实际截图尺寸**：在 Support 下调用截图，记录实际宽高、设备方向和截图归一化结果。
3. **校准输入坐标**：在 720p 截图中选取明显按钮，验证截图坐标传给 `post_click` 是否直接命中；若不一致，确定输入坐标反向转换规则。
4. **先迁移黑白数织**：完成动态网格、OCR ROI、格子中心、点击、滑动和完成验证的适配。
5. **再迁移彩色数织**：复用同一坐标适配层，迁移调色板、颜色采样、答案页和绘制读回。
6. **按主流程需要迁移模板**：只处理黑白和彩色数织入口及其成功/失败/返回状态，不一次性转换整个资源目录。
7. **接入主流程**：使用通用的黑白和彩色数织 CustomAction；独立 1080p 安全测试入口继续作为回归测试保留。
8. **逐步验收**：先离线截图，再 Support 实机；每次只改变一个坐标或资源层，避免算法、模板和 Pipeline 同时变化。

### 12.6 720p 迁移验收条件

黑白数织至少需要确认：

- 720p 下网格定位成功；
- 行列 OCR ROI 正确；
- 所有点击落在格子中心；
- 连续段滑动起止位置正确；
- 绘制完成后成功节点能够命中；
- 识别失败、求解失败和绘制失败仍被分开记录。

彩色数织至少需要确认：

- 720p 下完整 15×15 网格通过验证；
- 调色板数量、位置和颜色 ID 正确；
- 白色、浅色和相近颜色没有被错误过滤或混用；
- 正常求解和答案页反推都能生成同一格式的颜色矩阵；
- `allow_paint=false` 时不发生实际点击或滑动；
- 绘制后的局部读回和完整棋盘验证通过；
- 成功、答案兜底、绘制失败和完成态漏检分别生成正确阶段报告。

实机成功必须由 MaaFramework Support 的新日志或报告证明。720p 下的离线截图通过、MPE 通过和静态检查通过，都不能替代 Support 实机验收。

### 12.7 工作量控制和禁止事项

- 不要重写黑白和彩色数织求解器；优先新增统一显示配置和坐标转换层。
- 不要把 1080p 模板全部手工重做后才开始测试；先完成单按钮坐标校准和黑白数织最小闭环。
- 不要直接把所有固定数字乘以 `2/3` 后宣称迁移完成；模板匹配、OCR、网格线、颜色采样和输入坐标都需要实际验证。
- 不要覆盖历史 1080p 资源、报告和成功入口；不要将 720p 试验改动混入火车、汽船或其他无关主链。
- 不要用降低阈值、无条件重试或重复点击掩盖分辨率适配错误。
- 迁移期间每次修改后都要检查 `assets/` 与 `install/` 同步、Pipeline 引用、Agent 注册、JSON/Python 格式和针对性测试。

### 12.8 推荐后续实现任务

1. 新增统一 `DisplayProfile` 或等价的显示适配模块，集中处理逻辑坐标、实际截图尺寸、ROI 和输入坐标。
2. 给黑白数织增加 720p 截图尺寸和输入坐标诊断日志，但先不改变求解算法。
3. 将彩色数织 `_capture()`、调色板、答案页和绘制验证接入统一显示适配层。
4. 建立一组 720p 固定截图，覆盖正常黑白、正常彩色、答案页和失败状态。
5. 先用 `allow_paint=false` 做 720p 识别与矩阵验证，再开启绘制开关进行单幅实机验收。
6. 通过 Support 实机日志确认后，再将通用数织入口接入主流程。

本节记录的是迁移设计，不代表 720p 黑白和彩色数织已经完成实机验收。

## 13. 2026-08-30 交易所交换流程最新交接

### 13.1 当前目标流程

交易所交换入口仍使用已有的 `进入交易所交换`，入口点击交易所按钮后进入 `扫描交易所商品`。点击广告奖励流程与交易物品交换流程保持独立，不得因为交易所列表出现 `观看广告` 就直接点击广告。

当前交易物品顺序：

```text
木梁 → 钢铁 → 火药 → 铁砂 → 化学品
→ 观看广告图片+文字
→ 大幅滑动寻找贝多因人图片+文字
→ 150px 小幅滑动寻找海枣或异教徒
→ 识别异教徒后寻找加密币交换木材、石头
```

木材和石头暂时只允许以下目标：

- `加密币交换木材.png` → `交换木材`
- `加密币交换石头.png` → `交换石头`

以下普通兑换暂时禁用，不得重新接入：

- `木材交换石头.png`
- `石头交换木材.png`

### 13.2 交易所扫描 CustomAction

涉及文件：

- `agent/exchange_router.py`
- `install/agent/exchange_router.py`
- `assets/resource/pipeline/Pipeline7.json`
- `install/resource/pipeline/Pipeline7.json`
- `tests/test_exchange_router.py`

CustomAction 注册名为 `交易所商品扫描并跳转`，当前使用阶段状态：

- `before_bedouin`：扫描前五种商品，未命中时向上滑动 `100px`；不允许提前进入后续阶段。
- `search_bedouin`：只有当前画面没有前五种商品并且同时命中 `观看广告字样.png` 与 OCR `观看广告` 后进入；使用大幅滑动寻找贝多因人。
- `after_bedouin`：同时检查海枣和异教徒；找不到海枣时仍继续向上滑动 `150px`，不能把异教徒识别绑定到 `海枣 in processed`。
- `after_heretic`：只有异教徒图片+文字同时识别成功后，才允许扫描加密币交换木材、石头，使用 `150px` 小幅滑动。

阶段状态保存在 `_STAGE_STATE`，商品完成集合保存在 `_PROCESSED`，滑动次数保存在 `_SWIPE_COUNTS`。状态在成功、失败或异常时清理；新一轮任务必须确认没有继承上一轮阶段状态。

每个商品仍要求模板命中后进行 OCR；加密币商品必须在模板候选框内同时识别目标文字和 `加密币`，避免把不同兑换行拼接成“钢铁→第纳尔”或“铁砂→钢铁”。

### 13.3 当前滑动参数与实机问题

当前 Pipeline 参数：

- 前置小滑动：`small_step=100`。
- 后续小滑动：`gated_step=150`。
- 贝多因人阶段：当前配置为 `large_start_y=600`、`large_end_y=300`、`large_duration=600`、`large_max_swipes=8`。
- 每次扫描滑动后等待：`swipe_interval=900` 毫秒。
- 后续阶段总滑动上限：`gated_max_swipes=20`。

最近 Support 日志 `debug/maafw.log` 的交易所任务（约 `2026-08-30 14:49:16–14:49:33`）确认：

- 旧实现曾执行 `450→300` 的连续 `150px` 滑动，间隔过短，导致贝多因人可能被越过。
- 加密币石头模板在实际画面中的候选框为 `[337,608,602,96]`，最高分约 `0.622606`，低于原 `0.7` 阈值；因此虽然目标行已经出现，仍被判定为未命中，之后继续滑动并耗尽 `20` 次上限。
- 木材交换完成后返回 `扫描交易所商品`，说明交换弹窗返回列表的链路存在；后续必须以新的截图重新识别，不能假设仍停留在原位置。

针对上述日志已尝试的代码/配置调整：

- 贝多因人阶段改用约 `300px` 的 `600→300` 滑动，并在每次滑动后等待 `900ms`。
- 加密币木材、石头商品候选阈值暂降至 `0.55`，但仍要求目标文字和 `加密币` 行内 OCR 同时成立，不允许仅凭低分模板直接点击。
- 已补充 `import json`，避免 `_load_param()` 在实际运行时出现未定义名称。

注意：上述参数的代码检查已经通过，但新的 Support 实机运行尚未证明贝多因人不再被跳过，也尚未证明 `0.622606` 的石头候选能稳定完成交换。不能把这部分描述为实机已修复。

### 13.4 交易弹窗滑条与购买按钮

涉及文件：

- `agent/exchange_slider.py`
- `install/agent/exchange_slider.py`

当前方案不是固定点击坐标：

1. 先通过对应交易界面模板定位弹窗窗口；
2. 在窗口底部区域动态检测滑条；
3. 将滑条从左侧拖到右侧；
4. 在交易窗口内动态识别购买按钮，并在按钮区域 OCR `购买` 后点击；
5. 识别或点击失败时返回 `扫描交易所商品`，不得无限重试。

已知风险：化学品、海枣等交易窗口高度不同，滑条和购买按钮不能使用固定 ROI。加密币交易也必须继续使用动态窗口定位，不能恢复固定 `[485,437] → [780,437]` 坐标。

### 13.5 新增模板资源

已加入并同步到 `assets/resource/image/` 与 `install/resource/image/`：

- `观看广告字样.png`
- `贝多因人.png`
- `异教徒.png`

这些模板是标题/文字条局部图。使用时必须结合 OCR：

- `观看广告字样.png` + `观看广告`
- `贝多因人.png` + `贝多因人`
- `异教徒.png` + `异教徒`

不能只凭全屏 OCR 命中阶段文字，也不能把不同列表行的 OCR 结果拼接成商品判断。

### 13.6 当前验证记录

最近代码级验证结果：

- `tests/test_exchange_router.py`：`10 passed`。
- `python -m py_compile agent\\exchange_router.py install\\agent\\exchange_router.py`：通过。
- `assets/resource/pipeline/Pipeline7.json` 与 `install/resource/pipeline/Pipeline7.json`：JSON 解析通过。
- `agent/exchange_router.py` 与 `install/agent/exchange_router.py`：SHA256 一致。
- Pipeline 两份运行副本：SHA256 一致。
- `git diff --check`：通过；工作树中其他文件的 CRLF/LF 提示不属于本交易所改动。
- 未执行 Git commit、push 或发布操作。

### 13.7 下一步实机验收顺序

1. 从交易所入口重新开始，确认第一帧日志为 `phase=before_bedouin`，而不是继承 `after_bedouin` 或 `after_heretic`。
2. 确认前五种商品未完成时只出现 `100px` 小滑动。
3. 前五种商品均无法识别且出现 `观看广告` 图片+文字后，确认进入 `search_bedouin`，每次执行 `600→300`，并且间隔约 `900ms`。
4. 确认贝多因人图片+文字命中后进入 `after_bedouin`，随后以 `150px` 扫描海枣和异教徒；海枣暂时找不到时不得失败或停止检查异教徒。
5. 确认异教徒图片+文字命中后才扫描加密币木材、石头。
6. 检查石头候选分数低于 `0.7` 时是否能进入行内 OCR，并确认不会误点其他兑换行。
7. 木材或石头交换返回列表后，确认只重新截图识别，不立即重复滑动20次；每次滑动必须有日志和等待。
8. 最终报告必须区分 Support 实机日志、离线单元测试、静态检查和未验证项。

本节记录截至 `2026-08-30` 的交易所实现和问题现状，不代表最新一轮 Support 实机已经通过；后续应以新的 `debug/maafw.log` 和运行截图为准。
